<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "REC PARTS - Elevate your journey" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%# FontAwesome icons %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <%# Bootstrap JS %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <!-- Header Navigation -->
    <header class="border-bottom">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container d-flex align-items-center justify-content-between">
          <!-- Left Navigation -->
          <div class="navbar-nav me-auto d-none d-md-flex">
            <%= link_to "PRODUCTS", products_path, class: "nav-link text-dark text-uppercase" %>
            <%= link_to "FOR HARLEY", harley_path, class: "nav-link text-dark text-uppercase ms-4" %>
          </div>

          <!-- Center Logo -->
          <div class="navbar-brand mx-auto">
            <%= link_to root_path do %>
              <%= image_tag "logo.svg", alt: "REC PARTS - Elevate your journey", class: "img-fluid", style: "height: 36px;" %>
            <% end %>
          </div>

          <!-- Right Navigation -->
          <div class="navbar-nav ms-auto d-none d-md-flex">
            <%= link_to "SHOP", shop_path, class: "nav-link text-dark text-uppercase me-4" %>
            <%= link_to "CONTACT", contact_path, class: "nav-link text-dark text-uppercase" %>
          </div>

          <!-- Mobile Navigation -->
          <div class="d-md-none w-100">
            <div class="d-flex justify-content-center mt-2 gap-3">
              <%= link_to "PRODUCTS", products_path, class: "nav-link fw-bold text-dark text-uppercase small" %>
              <%= link_to "HARLEY", harley_path, class: "nav-link fw-bold text-dark text-uppercase small" %>
              <%= link_to "SHOP", shop_path, class: "nav-link fw-bold text-dark text-uppercase small" %>
              <%= link_to "CONTACT", contact_path, class: "nav-link fw-bold text-dark text-uppercase small" %>
            </div>
          </div>
        </div>
      </nav>
    </header>



    <!-- Main Content -->
    <main class="flex-grow-1 py-3">
      <%= yield %>
    </main>
    <footer class="footer d-flex justify-content-center align-items-end">
      <div class="copyright mb-1">&copy; <%= Time.current.year %> REC-PARTS All rights reserved.</div>
    </footer>
  </body>
</html>
